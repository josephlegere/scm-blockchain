const company = '';

const URL_SERVER = 'http://localhost:5000/'; //for live
const ROOT_DIR = '/'; // => /gemsaccounting

const SERVER_ATTR = {
    PAGE_MACHINE: URL_SERVER + 'api/v1/machines'
}

const RENDER_SOURCE = '#content-display';

export {
    SERVER_ATTR,
    RENDER_SOURCE
};